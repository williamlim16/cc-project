version: '3.1'

services:
  order:
      image: "queue"
      build: ./bun-react
      ports:
        - 3000:3000
      networks:
        - mainNetwork
  
  queue:
    image: "queue"
    build: ./go-ws
    ports:
      - 8000:8000
    networks:
      - mainNetwork

networks:
  mainNetwork: 
    driver: "bridge"
